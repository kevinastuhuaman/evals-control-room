# AGENTS.md

- Run `npm test` and `npm run build` before committing.
- Use merge-commit pull requests; never push directly to `main`.
- Keep all records synthetic and public-safe.
- Preserve keyboard access, reduced motion, and mobile views.
- Keep Pages/OIDC permissions scoped to the deploy job.
